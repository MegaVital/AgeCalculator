export interface DateFields {
    day: string,
    month: string,
    year: string
}
export interface AgeState {
    day: number,
    month: number,
    year: number
}

export interface ErrorMessages {
    day: string
    month: string
    year: string
}